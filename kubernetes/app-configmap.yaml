apiVersion: v1
kind: ConfigMap
metadata:
  name: app-configmap
data:
  ENV: "production"
  NEST_HOST: "nest-service"
  MYSQL_HOST: "mysql-service"
  MINIO_HOST: "minio-service"
  REDIS_HOST: "redis-service"
