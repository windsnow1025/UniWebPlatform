apiVersion: v1
kind: Service
metadata:
  name: kubernetes-dashboard-service
  namespace: kubernetes-dashboard
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: kong
  ports:
    - port: 8443
      targetPort: 8443
      protocol: TCP